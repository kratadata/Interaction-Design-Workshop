/*
Draw squares on the left side of the mouse and circles on the right sight of the mouse.
Vary the spacing by creating a custom variable.
*/

function setup(){
	createCanvas(800,800);	
	noStroke();
	rectMode(CENTER);
}

function draw(){
	background(120);
}